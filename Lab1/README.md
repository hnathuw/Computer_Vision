BÁO CÁO THỰC HÀNH XỬ LÝ ẢNH

1. Nội dung thực hiện:

* Đã chạy toàn bộ code trong file 2.1.1 (PIL) và 2.1.2 (OpenCV).
* Đã thêm giải thích chi tiết (comment) bên cạnh từng dòng lệnh trong cả 2 file.
* Đã liệt kê bảng so sánh đặc điểm giữa hai thư viện ở cuối file 2.1.2.

2. So sánh tóm tắt giữa PIL và OpenCV:

* Hệ màu: PIL dùng RGB (chuẩn), OpenCV dùng BGR (ngược kênh Đỏ-Lam).
* Dữ liệu: PIL dùng đối tượng riêng, OpenCV dùng mảng NumPy (tính toán nhanh hơn).
* Ứng dụng: PIL dùng cho chỉnh sửa ảnh cơ bản, OpenCV dùng cho thị giác máy tính nâng cao và video.

3. Kết luận:

* Đã nắm vững cách thao tác pixel và chuyển đổi hệ màu trên cả hai thư viện.
